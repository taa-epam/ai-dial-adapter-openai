name: Release Workflow

on:
  push:
    branches: [development, release-*]

jobs:
  release:
    uses: taa-epam/ai-dial-ci/.github/workflows/python_docker_release.yml@1280967b512e85ffc95fe6903a81eb9178f11966 #2.4.1
    secrets: inherit
    with:
      platforms: "linux/amd64,linux/arm64"