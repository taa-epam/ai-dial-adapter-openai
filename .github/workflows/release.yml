name: Release Workflow

on:
  push:
    branches: [development, release-*]

jobs:
  release:
    uses: taa-epam/ai-dial-ci/.github/workflows/python_docker_release.yml@455ef2441d6e6624b35097456704b0a181ea612f
    secrets: inherit
    strategy:
      fail-fast: false # Let all builds finish
      matrix:
        include:
          # - platform: "linux/amd64"
          #   runner: "ubuntu-24.04"
          #   arch: "amd64"
          - platform: "linux/arm64"
            runner: "ubuntu-24.04-arm"
            arch: "arm64"
            ort-bypassed: true
            trivy-enabled: false

          - platform: "linux/arm64"
            runner: "ubuntu-24.04"
            arch: "arm64"
            ort-bypassed: true
            trivy-enabled: false
    with:
      runs-on: '["${{ matrix.runner }}"]'
      platforms: "${{ matrix.platform }}"
      # maximize-build-space: true
      dockerhub-image-name: ltaa/ai-dial-adaoter-openai
      ort-bypassed: "${{ matrix.ort-bypassed }}"
      trivy-enabled: "${{ matrix.trivy-enabled }}"

